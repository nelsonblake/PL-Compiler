$ PL Program: Linear Search (valid)
begin
  const n = 10; 
  integer array A[n];
  integer x, i; Boolean found;

  $ Test other syntactic structures:
  proc Test
  begin
    const c = 1;
    const d = 2;
    integer e, f;
    e := -1-0;
    f := 1*c;
    e := 1/c;
    f := 1\d;
    if c > e & d < f -> write c; fi;
    if c = e | d = f -> write d; fi;
    skip;
  end;

  proc Search
  begin integer m;
    i, m := 1, n;
    do i<m ->
      if A[i] = x -> m := i; []
        ~(A[i] = x) -> i := i+1;
      fi;
    od;
    found := A[i] = x;
  end;

  $ Input Table:
  i := 1;
  do ~(i>n) -> read A[i]; i := i+1; od;
  
  $ Test Search:
  read x;
  do ~(x = 0) ->
    call Search;
    if found -> write x, i; []
      ~found -> write x;
    fi;
    read x;
  od;
end.
