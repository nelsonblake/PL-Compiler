$ PL Program: Linear Search (with errors, condensed)
begin
  const n = 10; integer array A[n];
  integer x, i; Boolean found;

  $ Test Scanner errors:
    const _badToken = 1;
    const badNumber = 65537;
    const invalidChar = @;

  proc Search
  $ Missing semicolon
  begin integer m
    i, m := 1, n;
    $ Missing od
    do i<m ->
      if A[i] = x -> m := i; []
        ~(A[i] = x) -> i := i+1;
      $ Missing fi
    found := A[i] = x;
  end;
end.
